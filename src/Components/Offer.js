import React from 'react'
const Offer = ({index, src, link}) => {
  return (
      // eslint-disable-next-line
    <> <a href={link}></a><img src={src} alt={`${index}offers`} /></>
  
    )
    
   
   
    
   
  
}

export default Offer